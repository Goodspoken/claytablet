# Трек: Пересборка Dev-контейнера и Выдача SSH (Rebuild)

**Цель:** Обеспечить агенту Antigravity автономность для прямого деплоя на VPS и переименовать проект в соответствии с ребрендингом.

## Задачи (выполняются агентом Grandfather на хосте):
- [ ] Остановить текущий dev-контейнер.
- [ ] Переименовать родительскую директорию `clip` (или `clipboard`) в `popycast`.
- [ ] Синхронизировать изменение через Syncthing между Gamebox и Serverbook.
- [ ] Прокинуть Volume с SSH-ключами хоста или скопировать приватный ключ `id_ed25519`/`id_rsa` для доступа к `admin@109.120.134.188` (VPS) внутрь контейнера.
- [ ] Переименовать имя контейнера в конфиге на `popycast-dev-box`.
- [ ] Запустить контейнер заново и проверить, что Antigravity (пользователь `vscode`) может беспрепятственно выполнять команду `ssh -p 2202 admin@109.120.134.188`.

**Связанный документ с промптом:** `conductor/rebuild_dev_prompt.md`
